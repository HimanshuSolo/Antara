numpy
opencv-python-headless
scikit-image
matplotlib
xarray
netCDF4
h5netcdf
boto3
tqdm
requests

# CPU-only PyTorch build -- this project only needs GPU for actual
# fine-tuning later (done on Colab/Kaggle), not for local pipeline work.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
# pinned to the +cpu build explicitly -- left unpinned, pip resolves a
# default (CUDA-linked) torchvision wheel that's ABI-incompatible with the
# +cpu torch build above and fails at import with "operator torchvision::nms
# does not exist".
torchvision

# perceptual (LPIPS) metric, to complement pixelwise PSNR/SSIM
lpips
